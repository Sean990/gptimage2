# gptimage2 完整 QA 报告

- 日期：2026-05-13
- 前端：`http://127.0.0.1:5173`
- 后端：`http://127.0.0.1:3001/api`
- 范围：首页、生成页、提示词优化器、定价页、案例库、文档页、法律页、个人中心、登录/注册/邀请链接、图库同步与删除
- 测试账号：`a1291955481@qq.com`，密码已按 `[REDACTED]` 处理

## 结论

最终健康评分：94/100。

本轮 QA 共发现并修复 4 个问题，其中 2 个为后端、2 个为前端。核心登录、个人中心、图库删除刷新、重新登录、邀请链接绑定、未登录触发登录提示均已复测通过。剩余风险为 e2e 中已有 moderate/minor 可访问性提示，不影响当前主流程。

## 修复列表

| ID | 严重级别 | 问题 | 修复状态 | 提交 |
| --- | --- | --- | --- | --- |
| QA-001 | Low | `AppFooter.vue` 不符合 Prettier，导致 `format:check` 失败 | 已验证 | `588f1c7` |
| QA-002 | High | 从 `127.0.0.1:5173` 打开前端时，后端 CORS 只允许 `localhost:5173`，登录被浏览器拦成网络错误 | 已验证 | `9a6c309` |
| QA-003 | High | `/me.galleryCount` 只统计 completed，和 `/gallery` 未删除记录数量不一致 | 已验证 | `3543573` |
| QA-004 | High | 删除同提示词同状态的云端失败记录后，本地兜底删除键会误隐藏另一条记录 | 已验证 | `f1e35d7` |

## 浏览器验证

- 公开页面：`/`、`/generate`、`/prompt-optimizer`、`/pricing`、`/showcase`、`/docs`、`/privacy-policy`、`/terms-of-service` 均可加载，控制台无错误。
- 登录流程：真实账号登录成功，用户菜单出现，控制台无错误。
- 图库删除：删除前 10 条，删除后 9 条；刷新后仍为 9；重新登录后仍为 9。
- 个人资料：图库作品数量显示 9，与 `/api/gallery` 和 `/api/me.galleryCount` 一致。
- 邀请链接：`/?inviteCode=QA-INVITE-20260513` 自动打开注册弹窗，邀请码已绑定、只读、不可修改。
- 未登录生成：点击“开始生成”显示“请先登录后再提交生成任务”，并打开登录弹窗。
- 移动端：390px 视口下首页和生成页可用，未见关键内容重叠或横向错位。

## 截图证据

- 首页：`screenshots/home.png`
- 生成页：`screenshots/generate.png`
- 图库删除前：`screenshots/gallery-before-delete.png`
- 图库删除后：`screenshots/gallery-after-delete.png`
- 修复后刷新：`screenshots/gallery-after-refresh-fixed.png`
- 重新登录后图库：`screenshots/gallery-after-relogin.png`
- 删除后个人资料：`screenshots/profile-after-gallery-delete.png`
- 邀请注册链接：`screenshots/invite-register-bound.png`
- 移动端首页：`screenshots/mobile-home-390.png`
- 移动端生成页：`screenshots/mobile-generate-390.png`

## 自动化检查

前端：
- `pnpm lint`：通过
- `pnpm format:check`：通过
- `pnpm test:unit`：通过，6 个文件，27 个用例
- `pnpm test:e2e`：通过，18 个用例

后端：
- `pnpm lint`：通过，49 个文件语法检查
- `pnpm db:check`：通过
- `pnpm smoke`：通过，auth/admin/真实生成按环境变量跳过
- `pnpm check:core`：通过
- `node scripts/check-cors-config.js`：通过
- `QA_AUTH_EMAIL=... QA_AUTH_PASSWORD=[REDACTED] node scripts/check-gallery-count.js`：通过，图库数量 9 条

其他：
- 前后端 `git diff --check`：通过

## 可访问性提示

`pnpm test:e2e` 仍报告若干 moderate/minor 提示，但没有严重问题：
- `landmark-complementary-is-top-level`
- `page-has-heading-one`

这些是现有 e2e 输出中的非阻塞提示，本轮未作为高优先级修复。

## 健康评分

| 类别 | 分数 | 说明 |
| --- | ---: | --- |
| Console | 100 | QA 页面和交互无控制台错误 |
| Links | 100 | 主要导航与页脚链接可达 |
| Functional | 95 | 登录、图库、邀请、未登录提示通过；真实生成未执行 |
| Visual | 95 | 桌面和 390px 移动视口未见关键布局问题 |
| UX | 92 | 关键提示明确；仍有少量 a11y 结构提示 |
| Performance | 90 | 未做 Lighthouse 性能深测，页面交互正常 |
| Content | 95 | 主要页面文案和状态可读 |
| Accessibility | 86 | e2e 存在 moderate/minor 提示 |

综合评分：94/100。

