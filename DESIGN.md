# ImgsGen Design System

## 依据与结论

本规范基于当前项目截图与样式实现归纳，主要参考：

- `artifacts/design-system-home-desktop-clean.png`
- `artifacts/design-system-generate-desktop-clean.png`
- `artifacts/design-system-generate-mobile-clean.png`
- `artifacts/generate-output-final-desktop.png`
- `src/assets/main.css`
- `src/assets/components.css`
- `src/assets/layout.css`
- `src/assets/home.css`
- `src/assets/generate.css`

### 产品气质

ImgsGen 是面向内容团队的 AI 图片生成与编辑工作台。气质应是“专业、轻盈、可信、效率优先”，不是玩具感的 AI 工具，也不是重运营后台。

适用场景：生成页、Prompt 优化、案例库、积分方案、登录注册、个人中心等所有前台页面。

不要怎么用：不要做成强营销落地页风格，不要使用过度装饰的紫色科技感，不要把核心工作流藏在大面积宣传内容后面。

### 目标用户

核心用户是内容运营、电商视觉、品牌社媒、设计协作人员，以及需要快速批量生成和复用素材的个人创作者。

适用场景：需要快速理解参数、上传参考图、查看生成结果、下载和复用历史任务。

不要怎么用：不要假设用户只浏览一次页面；工作台需要支持重复使用、扫描信息和快速操作。

### 核心使用场景

1. 首页理解产品能力并进入生成。
2. 生成页配置模型、模式、比例、分辨率、提示词和数量。
3. 结果区查看生成状态、对比、预览、下载、复制提示词、访问图库。
4. 移动端快速生成、查看结果区、打开历史记录。

适用场景：所有组件优先服务“参数输入 -> 生成 -> 查看/复用结果”的闭环。

不要怎么用：不要为了装饰增加步骤，不要让生成按钮、结果入口、历史图库入口在页面中失去优先级。

## 设计原则

### 1. 工作台优先

界面必须让用户第一眼看到当前任务、可编辑参数和结果区域。生成页桌面端采用左参数、右结果、左侧工具栏的三段式结构；移动端采用顶部品牌栏、工具快捷入口、当前模式摘要、参数卡片的顺序。

适用场景：生成页、局部改图、高清放大、自由扩图、抠图、消除等工具页。

不要怎么用：不要在生成页顶部放大幅营销 Hero；不要把结果区放到很深的位置；不要让说明文案超过操作控件的视觉权重。

### 2. 玻璃质感但保持可读

当前界面使用半透明白色、柔和边框、背景网格和轻微彩色环境光，形成轻量玻璃感。玻璃质感只用于导航、主面板、浮层、底部固定操作条。

适用场景：顶部导航、参数面板、结果面板、底部生成条、图库浮窗、Modal。

不要怎么用：不要在所有小控件上叠加 backdrop-filter；不要让背景图片透过正文输入框影响可读性；不要把玻璃卡片套在另一张玻璃卡片里。

### 3. 以蓝色表达主操作，以橙色表达成本/积分

蓝色是生成、选择、当前状态、焦点的主色；橙色是积分、消耗、即将上线、合规提醒等成本或注意信息。这个二元关系在截图中已经形成稳定认知。

适用场景：主按钮、激活 Tab、聚焦边框、导航当前项使用蓝色；积分胶囊、消耗按钮、提醒徽标使用橙色。

不要怎么用：不要用橙色做普通主按钮；不要用蓝色提示积分消耗；不要新增没有语义的第三个大面积强调色。

### 4. 大控件圆润，小控件克制

品牌导航和主按钮使用胶囊形态，工作台卡片使用 14-24px 圆角，列表项、菜单项和小输入控件使用 8-14px 圆角。

适用场景：CTA、数量分段、胶囊徽标、导航外壳、面板、工具卡片。

不要怎么用：不要所有元素都用 999px；不要让表单输入、菜单项和内容卡片使用同一圆角；不要随意出现 7px、11px、22px 等一次性值。

### 5. 操作反馈轻而确定

公共按钮当前有悬浮上移，生成页又取消了多数组件位移。规范应统一为：工作台内不使用大幅位移，营销内容可轻微上浮。

适用场景：工作台按钮、图像操作按钮、分段控件、下拉菜单、移动端底部操作条。

不要怎么用：不要在高频工具按钮上使用跳动、缩放或较大位移；不要让 hover 改变布局尺寸；不要在移动端依赖 hover 才显示关键操作。

## 色彩系统

### 基础色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#f8fafc` | 页面底色 |
| `--surface` | `#ffffff` | 主面板、卡片、输入底色 |
| `--surface-soft` | `#f1f5f9` | 弱背景、悬浮背景、结果区底色 |
| `--surface-sunk` | `#e2e8f0` | 凹陷区域、分割底 |
| `--line` | `#e4e9f1` | 默认边框 |
| `--line-strong` | `#cdd5e1` | hover、强调边框 |
| `--text-strong` | `#0f172a` | 标题、重要数字 |
| `--text` | `#1e293b` | 正文与控件文字 |
| `--muted` | `#475569` | 次级说明 |
| `--subtle` | `#64748b` | 占位、辅助信息 |

适用场景：所有页面默认使用这组中性蓝灰色，保证干净、专业、耐看。

不要怎么用：不要引入纯黑 `#000` 作为正文；不要在浅色背景上使用低于 `#64748b` 的小字号说明；不要用暖灰替换主中性色。

### 品牌色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--primary` | `#2563eb` | 主按钮、激活状态、聚焦 |
| `--primary-strong` | `#1d4ed8` | 主按钮渐变底、强调文字 |
| `--primary-soft` | `rgba(37, 99, 235, 0.10)` | 激活背景、浅色徽标 |
| `--primary-ring` | `rgba(37, 99, 235, 0.28)` | focus ring |
| `--accent` | `#f97316` | 积分、成本、即将上线 |
| `--accent-strong` | `#ea580c` | 橙色强调文字 |

适用场景：蓝色用于用户想要推进任务的动作；橙色用于告知代价、限制、风险和积分。

不要怎么用：不要混用 `#2563eb` 与 `rgba(47, 102, 245, ...)` 两套蓝色基准；不要让紫粉渐变成为默认 CTA；不要在同一区域同时出现蓝、橙、紫、青四个强调色。

### 语义色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--success` | `#16a34a` | 成功、已保存 |
| `--warn` | `#d97706` | 警告、成本提示 |
| `--danger` | `#dc2626` | 错误、删除、失败 |
| `--info` | `#0ea5e9` | 信息提示 |

适用场景：Toast、表单消息、合规提示、上传状态、任务状态。

不要怎么用：不要用成功绿作为装饰色；不要用红色表示普通取消；不要只靠颜色表达状态，必须配合文字或图标。

### 背景光效

当前背景由浅色线性底、蓝橙青径向光和细网格构成。它是品牌环境，不是内容主体。

适用场景：首页首屏、生成页背景、公共页面顶层背景。

不要怎么用：不要添加离散彩色圆球、bokeh 光斑或强紫色渐变；不要让背景纹理进入卡片内部；不要在结果图片区域上叠背景效果。

## 字体系统

### 字体族

主字体使用：

```css
font-family:
  'Plus Jakarta Sans',
  'Inter',
  'PingFang SC',
  'Microsoft YaHei',
  'Noto Sans SC',
  system-ui,
  sans-serif;
```

适用场景：产品 UI、中文正文、参数标签、按钮、导航。

不要怎么用：不要在同一页面混入装饰字体；不要为中文标题单独使用宋体或书法字体；不要用等宽字体承载普通参数。

### 字号

| Token | 值 | 用途 |
| --- | --- | --- |
| `--fs-xs` | `12px` | 小标签、说明、meta |
| `--fs-sm` | `13px` | 次级说明、徽标 |
| `--fs-md` | `14px` | 表单、按钮、导航 |
| `--fs-lg` | `16px` | 正文、生成页说明 |
| `--fs-xl` | `18px` | 段落引导 |
| `--fs-2xl` | `22px` | 面板标题 |
| `--fs-title-sm` | `24px` | Modal 标题、工作台主标题 |
| `--fs-title-md` | `32px` | 页面小标题 |
| `--fs-hero` | `clamp(36px, 5.4vw, 64px)` | 首页 Hero |

适用场景：工作台内部以 12-24px 为主，首页 Hero 才使用大字号。

不要怎么用：不要在卡片和工具栏中使用 Hero 字号；不要用 `vw` 缩放控件文字；不要在按钮中使用小于 13px 的中文文本。

### 字重

| Token | 值 | 用途 |
| --- | --- | --- |
| `--fw-regular` | `400` | 正文 |
| `--fw-medium` | `500` | 导航、辅助 |
| `--fw-semibold` | `600` | 按钮、标签 |
| `--fw-bold` | `700` | 标题、重点 |
| `--fw-black` | `800` | 少量数字、强标题 |

适用场景：中文界面优先使用 500-700，减少糊成一团的粗黑。

不要怎么用：不要再新增 `900`、`950` 这类硬编码字重；不要让小标签和按钮全部 900；不要用过粗字重弥补层级不足。

### 行高与字距

正文行高使用 `1.65-1.8`，控件行高使用 `1.2-1.35`，标题行高使用 `1.08-1.25`。字距默认 `0`，小标签最多 `0.02em`。

适用场景：说明文本、Prompt 文本域、帮助文字、导航和徽标。

不要怎么用：不要使用负字距；不要让中文段落行高低于 `1.5`；不要在长中文句子里使用过大的字距。

## 间距与栅格

### 基础间距

采用 4px 基准，但对业务布局暴露 8px 主节奏。

| Token | 值 | 用途 |
| --- | --- | --- |
| `--space-1` | `4px` | 极小间隔 |
| `--space-2` | `8px` | 图标与文字、菜单项内部 |
| `--space-3` | `12px` | 小控件间距 |
| `--space-4` | `16px` | 卡片内基础留白 |
| `--space-5` | `20px` | 面板内留白 |
| `--space-6` | `24px` | 卡片组间距 |
| `--space-8` | `32px` | 页面模块 |
| `--space-12` | `48px` | 首页区块 |
| `--space-16` | `64px` | 大区块 |
| `--space-19` | `76px` | 当前 section 默认竖向间距 |

适用场景：工作台用 8/12/14/16/20/24px 的密集节奏；首页内容区可用 32/48/64/76px。

不要怎么用：不要在组件内随意出现 11px、18px、22px、28px；不要让移动端容器左右留白低于 10px；不要用 margin 造视觉对齐，优先用 grid/flex gap。

### 容器与栅格

| 场景 | 规则 |
| --- | --- |
| 公共容器 | `width: min(1180px, calc(100% - 40px))` |
| 生成页容器 | `width: min(1520px, calc(100% - 32px))` |
| 生成页桌面 | 左工具栏 72px，主区域左右两栏，左参数约 520-640px，右结果自适应 |
| 首页 Hero | 两栏，文案约 1.02fr，视觉约 0.98fr，gap 48px |
| 移动端 | 单列，核心控件宽度撑满，快捷工具横向 5 等分 |

适用场景：首页、生成页、内容页、价格页、案例页。

不要怎么用：不要让生成页主容器套用 1180px 宽度；不要在桌面生成页把参数区压到 420px 以下；不要在移动端保留双栏。

## 圆角、边框、阴影

### 圆角

| Token | 值 | 用途 |
| --- | --- | --- |
| `--radius-xs` | `6px` | 极小图标、微型控件 |
| `--radius-sm` | `10px` | 小按钮、图标底 |
| `--radius-md` | `14px` | 输入框、菜单、普通卡片 |
| `--radius-lg` | `18px` | 参数面板、浮层 |
| `--radius-xl` | `24px` | 大 Modal、Hero 图片 |
| `--radius-pill` | `999px` | 主按钮、导航、徽标 |

适用场景：圆角按组件尺寸递增。越高频的工作控件越克制，越品牌化的容器越圆润。

不要怎么用：不要在新增组件中使用未定义圆角；不要把结果画布、图片预览和输入框都做成同一种大圆角；不要出现卡片 22px、CTA 8px 这类无体系值。

### 边框

默认边框使用 `1px solid var(--line)`；hover 或 active 使用 `var(--line-strong)` 或 `primary` 的透明色；选中状态使用浅蓝背景加蓝色边框。

适用场景：卡片、输入框、面板、工具按钮、分段控件。

不要怎么用：不要用纯黑或纯蓝 1px 边框包围大面积容器；不要只靠背景色表示选中；不要在同一组控件里混用实线、虚线和无边框。

### 阴影

| Token | 值 | 用途 |
| --- | --- | --- |
| `--shadow-xs` | `0 1px 2px rgba(15, 23, 42, 0.05)` | 小控件 |
| `--shadow-sm` | `0 4px 12px rgba(15, 23, 42, 0.06)` | 导航、普通卡片 |
| `--shadow-md` | `0 10px 24px rgba(15, 23, 42, 0.08)` | 工作台面板、固定操作条 |
| `--shadow-lg` | `0 18px 40px rgba(15, 23, 42, 0.10)` | 浮层 |
| `--shadow-xl` | `0 28px 60px rgba(15, 23, 42, 0.14)` | Modal、Hero 图 |
| `--shadow-primary` | `0 12px 28px rgba(37, 99, 235, 0.22)` | 主按钮 |
| `--shadow-accent` | `0 12px 28px rgba(249, 115, 22, 0.22)` | 积分按钮 |

适用场景：用阴影表达层级，导航和固定按钮需要比普通卡片更明显。

不要怎么用：不要每个卡片 hover 都大幅抬升；不要新增硬编码深阴影；不要让阴影颜色脱离蓝灰/品牌色体系。

## 核心组件规范

### 顶部导航

固定在顶部，外壳为胶囊玻璃容器，桌面宽度跟随公共容器，品牌在左，导航居中，主题/登录/积分在右。滚动后提高背景不透明度和阴影。

适用场景：首页、生成页、内容页全局导航。

不要怎么用：不要改为全宽实色导航；不要让导航项超过一行；不要在右侧堆叠超过 3 个主要操作。

### 品牌标识

品牌图标使用 32px 方形渐变图标底，圆角 10px，蓝橙渐变；文字为 `ImgsGen`，字重 700。

适用场景：导航、页脚、登录弹窗、空状态品牌露出。

不要怎么用：不要改为纯文字；不要使用过大的 Logo；不要在同一页面使用多套 Logo 色。

### 按钮

按钮分为 Primary、Accent、Soft、Ghost、Icon。

| 类型 | 视觉 | 适用场景 | 不要怎么用 |
| --- | --- | --- | --- |
| Primary | 蓝色纵向渐变，胶囊，白字 | 开始生成、确认、提交 | 不要用于积分消耗或普通导航 |
| Accent | 橙色渐变，胶囊，白字 | 重要成本动作、促销确认 | 不要和 Primary 并列抢主操作 |
| Soft | 浅蓝底、蓝字 | 次级操作、当前导航、轻按钮 | 不要用于危险操作 |
| Ghost | 白底、浅边框 | 查看、取消、辅助入口 | 不要作为页面唯一 CTA |
| Icon | 40x40 胶囊或圆形 | 主题切换、菜单、下载、复制 | 不要用文字替代常见图标 |

适用场景：所有可点击命令。

不要怎么用：不要在按钮内放过长中文；不要让按钮高度低于 40px，移动端低于 44px；不要用紫粉渐变作为默认主按钮，当前“一键优化”可作为特定 AI 辅助动作例外。

### 参数面板

桌面端参数面板为白色大卡片，圆角 18-24px，内边距 20px，按“模式 -> 基础参数 -> Prompt -> 输出参数 -> 生成条”组织。移动端拆成摘要卡和参数卡。

适用场景：生成参数、图片工具参数、Prompt 优化参数。

不要怎么用：不要在单个面板里无限堆叠分组；不要让关键生成按钮在首屏不可见；不要把说明文字放在标签之前。

### 模式卡片与分段控件

模式卡片使用浅蓝选中态、蓝色边框、双行文字；数量选择使用胶囊分段，选中项为蓝色实底带阴影。

适用场景：文生图/图生图/精修图、图片数量、工具类型。

不要怎么用：不要只用文字颜色表示选中；不要让选中项改变整体高度；不要在同一分组混合卡片和胶囊两种样式。

### 输入框与文本域

输入框默认白底、14px 圆角、浅边框；聚焦时蓝色边框加 4px 光环。Prompt 文本域桌面最小高度 160px，移动端约 132-160px。

适用场景：Prompt、图片 URL、账号表单、搜索。

不要怎么用：不要使用透明输入框；不要移除 focus ring；不要让 textarea 在移动端高于首屏主要操作区域。

### 结果面板

结果面板是右侧核心画布，使用白色外层面板、浅灰结果工作区、内部图片容器。空状态居中显示图标、标题、说明；生成成功后显示结果操作条和最近任务。

适用场景：AI 生成结果、对比、图层、编辑历史、下载预览。

不要怎么用：不要把结果图直接贴在页面背景上；不要在图片上永久遮挡大面积操作按钮；不要让空状态过度营销化。

### 左侧工具栏

桌面端左侧工具栏宽 72px，图标上、文字下，当前工具使用对应工具色浅底和实色图标块。移动端转为顶部五等分工具快捷入口。

适用场景：创作、高清、扩图、抠图、消除等工具切换。

不要怎么用：不要在桌面端使用横向大工具卡占用工作区；不要让工具文字超过两字或三字短标签；不要让移动端横向滚动隐藏关键工具。

### 图库浮窗

右下角固定，白色胶囊卡片，蓝色图标块，展示“图库/我的图库/任务数量”。移动端应避开底部操作条和安全区。

适用场景：全站访问历史任务、生成页快速打开图库。

不要怎么用：不要遮挡“开始生成”按钮；不要在弹窗打开时仍保留页面滚动穿透；不要把图库入口做成普通导航链接。

### Modal 与 Toast

Modal 使用半透明深色遮罩加 6px blur，卡片圆角 24px，最大宽 440-620px。Toast 顶部居中，胶囊形态，配状态图标。

适用场景：地区限制、登录注册、预览、确认、全局反馈。

不要怎么用：不要把长表单塞进小 Modal；不要让 Toast 盖住顶部导航核心操作；不要使用没有图标的错误提示。

## 状态规范

### 默认状态

默认状态应保持低噪声：白底、浅边框、中性文字。

适用场景：未选择卡片、输入框、Ghost 按钮、普通面板。

不要怎么用：不要默认就使用彩色边框或高阴影。

### Hover 状态

工作台内 hover 只改变背景、边框和阴影，不做明显位移；营销卡片可使用 `translateY(-4px)`。

适用场景：工具按钮、结果操作按钮、菜单项、首页特性卡。

不要怎么用：不要在生成页高频按钮上使用 `scale(1.01)` 或大幅上浮；不要让 hover 后文字换行。

### Active/Selected 状态

选中态使用浅蓝背景、蓝色边框、蓝色文字；主分段选中可用蓝色实底白字。

适用场景：导航当前项、模式卡片、数量分段、下拉选项、工具当前项。

不要怎么用：不要只用图标颜色变化表达选中；不要选中后改变控件尺寸。

### Focus 状态

键盘焦点使用 `2px solid var(--primary)` 或组件内 `0 0 0 4px var(--primary-ring)`。

适用场景：所有 button、link、input、textarea、select、summary。

不要怎么用：不要移除 outline；不要让 focus ring 被 overflow 裁切。

### Disabled 状态

禁用态使用 `var(--surface-soft)` 背景、`var(--subtle)` 文字、无阴影、无 transform，鼠标为 `not-allowed`。

适用场景：不可提交、生成中、上传未满足条件、数量不可选。

不要怎么用：不要只降低透明度；不要保留 hover 阴影；不要让禁用态看起来仍可点击。

### Loading 状态

加载状态应明确说明当前阶段，使用旋转图标或骨架屏。生成区应保留结果画布尺寸，避免布局跳动。

适用场景：生成中、图库加载、异步组件加载、上传处理。

不要怎么用：不要用整页空白等待；不要让底部操作条在 loading 时突然改变宽度。

### Error 状态

错误使用红色语义色、浅红背景、图标和明确文案。需要给出下一步动作。

适用场景：生成失败、上传失败、登录失败、接口失败。

不要怎么用：不要只显示英文异常；不要用 Toast 替代所有表单内错误；不要把错误色用于普通警告。

## 图标与插画规范

### 图标

项目已使用 `lucide-vue-next`，新增 UI 图标应继续使用 Lucide。图标默认 18-20px，小标签 15-17px，大状态图标 28-48px。

适用场景：按钮、工具栏、状态、空状态、导航、表单辅助。

不要怎么用：不要手写 SVG 替代 Lucide 已有图标；不要混用多套图标库；不要使用没有文字标签的陌生图标，除非有 tooltip 或 aria-label。

### 图片与插画

首页必须使用真实生成结果或高质量示例图，当前四宫格人物图符合“展示结果能力”的方向。结果区使用真实生成结果、缩略图和任务图，不使用抽象插画代替。

适用场景：首页 Hero、案例库、最近任务、结果预览。

不要怎么用：不要使用纯 SVG 科技插画作为核心视觉；不要使用模糊、过暗、无法判断生成质量的图片；不要让装饰图抢过生成结果本身。

## 禁用规则与设计红线

1. 禁止新增无语义的紫色/粉色大面积渐变。适用场景：所有 CTA 和页面背景。不要怎么用：除“一键优化”这类 AI 辅助动作外，不得把紫粉作为主品牌。
2. 禁止卡片套卡片。适用场景：参数面板、结果面板、首页模块。不要怎么用：不要把整个页面 section 做成浮动卡片后内部再放卡片。
3. 禁止隐藏核心操作。适用场景：生成页和移动端。不要怎么用：开始生成、结果区、图库入口不得被浮窗遮挡。
4. 禁止字号随视口宽度缩放普通 UI。适用场景：按钮、标签、输入框、菜单。不要怎么用：`vw` 只可用于 Hero 标题 clamp。
5. 禁止使用未定义 token 的一次性颜色、圆角、阴影。适用场景：所有新增 CSS。不要怎么用：不要继续堆硬编码 `rgba(47, 102, 245, ...)`、`22px`、`950` 等。
6. 禁止依赖 hover 才能完成移动端操作。适用场景：图片结果操作、工具按钮。不要怎么用：移动端必须默认展示关键操作。
7. 禁止让长中文文本溢出按钮或卡片。适用场景：导航、工具卡、徽标、Toast。不要怎么用：不要用 `white-space: nowrap` 强压不可控长文本。
8. 禁止弱化可访问焦点。适用场景：键盘可达元素。不要怎么用：不要写 `outline: none` 后不补焦点样式。

## 当前不一致点

1. 蓝色基准不统一：全局 token 是 `#2563eb`，但大量透明色使用 `rgba(47, 102, 245, ...)`，对应约 `#2f66f5`。应统一到 `37, 99, 235`。
2. 圆角体系漂移：全局有 6/10/14/18/24/999，但实际出现 8、12、22、7 等硬编码。应收敛到 token。
3. 字重过重：部分工具卡使用 `900`、`950`，与 `--fw-black: 800` 冲突，中文小字显得压迫。
4. 交互反馈冲突：公共 `.btn` 和 `.card` hover 会上浮，但生成页又显式取消 transform。应定义“营销可上浮、工作台不位移”的规则。
5. 强调色过多：主品牌蓝橙之外，工具栏有青、蓝、绿、棕、紫；批量模式还有紫蓝渐变。工具色可以保留，但只应用于图标和浅底，不应扩散到大 CTA。
6. 阴影硬编码过多：导航、工具卡、Toast、浮窗各自写独立阴影，层级关系不稳定。应映射到 `--shadow-*`。
7. 空状态样式不统一：有的空状态是 8px 虚线框，有的结果区是 14-24px 软面板。工作台空状态应优先使用结果区软面板语言。
8. 移动端底部固定条与图库浮窗存在遮挡风险。当前截图中图库浮窗能避开部分区域，但新增浮层必须遵守安全区和底部操作条避让。

## Design Tokens

### CSS Variables

```css
:root {
  --bg: #f8fafc;
  --surface: #ffffff;
  --surface-soft: #f1f5f9;
  --surface-sunk: #e2e8f0;
  --line: #e4e9f1;
  --line-strong: #cdd5e1;

  --text-strong: #0f172a;
  --text: #1e293b;
  --muted: #475569;
  --subtle: #64748b;

  --primary: #2563eb;
  --primary-rgb: 37 99 235;
  --primary-strong: #1d4ed8;
  --primary-soft: rgb(var(--primary-rgb) / 0.1);
  --primary-ring: rgb(var(--primary-rgb) / 0.28);

  --accent: #f97316;
  --accent-rgb: 249 115 22;
  --accent-strong: #ea580c;
  --accent-soft: rgb(var(--accent-rgb) / 0.1);

  --success: #16a34a;
  --warn: #d97706;
  --danger: #dc2626;
  --info: #0ea5e9;

  --radius-xs: 6px;
  --radius-sm: 10px;
  --radius-md: 14px;
  --radius-lg: 18px;
  --radius-xl: 24px;
  --radius-pill: 999px;

  --shadow-xs: 0 1px 2px rgb(15 23 42 / 0.05);
  --shadow-sm: 0 4px 12px rgb(15 23 42 / 0.06);
  --shadow-md: 0 10px 24px rgb(15 23 42 / 0.08);
  --shadow-lg: 0 18px 40px rgb(15 23 42 / 0.10);
  --shadow-xl: 0 28px 60px rgb(15 23 42 / 0.14);
  --shadow-primary: 0 12px 28px rgb(var(--primary-rgb) / 0.22);
  --shadow-accent: 0 12px 28px rgb(var(--accent-rgb) / 0.22);

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-19: 76px;

  --fs-xs: 12px;
  --fs-sm: 13px;
  --fs-md: 14px;
  --fs-lg: 16px;
  --fs-xl: 18px;
  --fs-2xl: 22px;
  --fs-title-sm: 24px;
  --fs-title-md: 32px;
  --fs-hero: clamp(36px, 5.4vw, 64px);

  --fw-regular: 400;
  --fw-medium: 500;
  --fw-semibold: 600;
  --fw-bold: 700;
  --fw-black: 800;

  --ease-standard: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-emphasized: cubic-bezier(0.16, 1, 0.3, 1.03);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);

  --dur-1: 150ms;
  --dur-2: 240ms;
  --dur-3: 320ms;
  --dur-4: 450ms;

  --container: 1180px;
  --container-generate: 1520px;
  --page-gutter: 20px;
  --page-gutter-compact: 16px;
}

:root[data-theme='dark'] {
  --bg: #060a14;
  --surface: #111a2c;
  --surface-soft: #16213a;
  --surface-sunk: #0c1322;
  --line: #25324c;
  --line-strong: #344768;

  --text-strong: #f1f6ff;
  --text: #e5eefc;
  --muted: #b3bdd1;
  --subtle: #8a96b0;

  --primary: #5b86cf;
  --primary-rgb: 91 134 207;
  --primary-strong: #88a9df;
  --accent: #fb923c;
  --accent-rgb: 251 146 60;
  --accent-strong: #fdba74;

  --success: #4ade80;
  --warn: #fbbf24;
  --danger: #f87171;
  --info: #38bdf8;

  --shadow-xs: 0 1px 2px rgb(0 0 0 / 0.40);
  --shadow-sm: 0 6px 16px rgb(0 0 0 / 0.32);
  --shadow-md: 0 12px 28px rgb(0 0 0 / 0.40);
  --shadow-lg: 0 22px 50px rgb(0 0 0 / 0.50);
  --shadow-xl: 0 32px 70px rgb(0 0 0 / 0.58);
}
```

### Tailwind Theme 映射

```js
export default {
  theme: {
    extend: {
      colors: {
        bg: 'var(--bg)',
        surface: 'var(--surface)',
        'surface-soft': 'var(--surface-soft)',
        'surface-sunk': 'var(--surface-sunk)',
        line: 'var(--line)',
        'line-strong': 'var(--line-strong)',
        strong: 'var(--text-strong)',
        text: 'var(--text)',
        muted: 'var(--muted)',
        subtle: 'var(--subtle)',
        primary: 'var(--primary)',
        'primary-strong': 'var(--primary-strong)',
        accent: 'var(--accent)',
        'accent-strong': 'var(--accent-strong)',
        success: 'var(--success)',
        warn: 'var(--warn)',
        danger: 'var(--danger)',
        info: 'var(--info)',
      },
      borderRadius: {
        xs: 'var(--radius-xs)',
        sm: 'var(--radius-sm)',
        md: 'var(--radius-md)',
        lg: 'var(--radius-lg)',
        xl: 'var(--radius-xl)',
        pill: 'var(--radius-pill)',
      },
      boxShadow: {
        xs: 'var(--shadow-xs)',
        sm: 'var(--shadow-sm)',
        md: 'var(--shadow-md)',
        lg: 'var(--shadow-lg)',
        xl: 'var(--shadow-xl)',
        primary: 'var(--shadow-primary)',
        accent: 'var(--shadow-accent)',
      },
      spacing: {
        1: 'var(--space-1)',
        2: 'var(--space-2)',
        3: 'var(--space-3)',
        4: 'var(--space-4)',
        5: 'var(--space-5)',
        6: 'var(--space-6)',
        8: 'var(--space-8)',
        12: 'var(--space-12)',
        16: 'var(--space-16)',
        19: 'var(--space-19)',
      },
      fontSize: {
        xs: 'var(--fs-xs)',
        sm: 'var(--fs-sm)',
        md: 'var(--fs-md)',
        lg: 'var(--fs-lg)',
        xl: 'var(--fs-xl)',
        '2xl': 'var(--fs-2xl)',
        'title-sm': 'var(--fs-title-sm)',
        'title-md': 'var(--fs-title-md)',
        hero: 'var(--fs-hero)',
      },
      fontWeight: {
        regular: 'var(--fw-regular)',
        medium: 'var(--fw-medium)',
        semibold: 'var(--fw-semibold)',
        bold: 'var(--fw-bold)',
        black: 'var(--fw-black)',
      },
      transitionTimingFunction: {
        standard: 'var(--ease-standard)',
        emphasized: 'var(--ease-emphasized)',
        spring: 'var(--ease-spring)',
      },
      transitionDuration: {
        1: 'var(--dur-1)',
        2: 'var(--dur-2)',
        3: 'var(--dur-3)',
        4: 'var(--dur-4)',
      },
      maxWidth: {
        container: 'var(--container)',
        generate: 'var(--container-generate)',
      },
    },
  },
}
```

